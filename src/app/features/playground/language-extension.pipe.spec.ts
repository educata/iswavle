import { LanguageExtensionPipe } from './language-extension.pipe';

describe('LanguageExtensionPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageExtensionPipe();
    expect(pipe).toBeTruthy();
  });
});
