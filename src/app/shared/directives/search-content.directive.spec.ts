import { SearchContentDirective } from './search-content.directive';

describe('SearchContentDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchContentDirective();
    expect(directive).toBeTruthy();
  });
});
